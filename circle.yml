machine:
    environment:
        PATH: /home/ubuntu/miniconda2/bin:$PATH
        TEST_CI: "True"
        PY_VERSIONS: "3.6"
        CONDA_DEPENDENCIES: "pyqt=5.*"

dependencies:
    pre:
        - pip install -r requirements.txt